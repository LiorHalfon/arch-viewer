# core's own rules (M8 fixture): plugin has none, deliberately, to exercise
# "a workspace package without its own rules file".
[archview]
package = "core"
source_roots = ["src"]

[archview.allowed]
model = []
ports = ["model"]

/** `core`, reached three ways: a relative path, its scoped npm name, its bare name. */
import { Widget } from "../../core/src/index";
import { helper } from "@fixture/core";
import { helper2 } from "core";

export const summary = { Widget, helper, helper2 };

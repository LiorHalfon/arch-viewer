[project]
name = "archview"
version = "0.1.1"
description = "Architecture viewer and dependency-rules checker for Python code bases"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Lior Halfon" }]
dependencies = [
    "fastapi>=0.141.1",
    "grimp>=3.16",
    "networkx>=3.4",
    "uvicorn>=0.53.0",
]

[project.scripts]
archview = "archview.cli:main"

[dependency-groups]
dev = [
    "pytest>=8.3",
    "ruff>=0.14",
    "jsonschema>=4.23",
    "httpx2>=2.13.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/archview"]

[tool.ruff]
line-length = 100
target-version = "py312"
# The fixture project is deliberately odd code, pinned by line number in the golden
# files; `spike/` is exploratory and not held to the product's standards.
extend-exclude = ["tests/fixtures", "spike", "docs"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
# Starlette's own test client, not our code.
filterwarnings = ["ignore:The anyio.abc.BlockingPortal alias:DeprecationWarning"]
